# Contexta - Telegram Mini App для изучения языков 🌍

**Contexta** — это приложение внутри Telegram (Mini App), которое помогает изучать иностранные языки через контекстные истории, сгенерированные искусственным интеллектом (AI).

## 🌟 Основные возможности

*   **Генерация историй:** Введите слово или фразу, выберите язык и уровень сложности (A1-C1). AI создаст короткую историю с использованием этого слова.
*   **Контекстное обучение:** Изучайте слова в контексте, а не изолированно.
*   **Подсветка слов:** Ключевые слова в тексте выделяются для удобства.
*   **Изучение нескольких языков:** Поддержка Английского (US), Португальского (EU), Испанского и Польского.
*   **Темная тема:** Стильный и современный дизайн, приятный для глаз.

## 🛠 Технологический стек

*   **Frontend:** React, Vite, TypeScript, Tailwind-like CSS variables (Dark Theme).
*   **Backend:** Python 3.11, FastAPI, SQLAlchemy, Pydantic.
*   **Database:** PostgreSQL (хранение пользователей и историй).
*   **Cache/Rate Limiting:** Redis (ограничение запросов).
*   **AI:** Groq API (модель Mixtral/Llama).
*   **Bot:** Aiogram 3.x.
*   **Infrastructure:** Docker & Docker Compose.

---

## 🚀 Как запустить локально

Для запуска вам понадобятся установленные **Docker** и **Docker Compose**.

### 1. Клонирование репозитория

```bash
git clone https://github.com/kukhmax/contexta_tg.git
cd contexta_tg
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта на основе примера.
Вам понадобятся:
1.  **Токен бота:** Получите у [@BotFather](https://t.me/BotFather).
2.  **API ключ Groq:** Получите на [console.groq.com](https://console.groq.com).

Пример `.env`:

```env
DATABASE_URL=postgresql://postgres:postgres@db:5432/contexta
REDIS_URL=redis://redis:6379/0
BOT_TOKEN=ваш_токен_бота_от_BotFather
GROQ_API_KEY=ваш_ключ_groq_api
```

### 3. Запуск контейнеров

```bash
docker-compose up -d --build
```
Эта команда соберет и запустит бэкенд, фронтенд, базу данных, Redis и бота.

### 4. Применение миграций БД

При первом запуске необходимо создать таблицы в базе данных:

```bash
docker-compose run --rm backend alembic upgrade head
```

---

## 📱 Как тестировать в Telegram

### Локальное тестирование (Localhost)

Телеграм не может напрямую открывать `localhost` ссылки в Web App. Для тестирования Mini App локально есть два пути:

#### Вариант А: Эмуляция в браузере (Рекомендуемый для разработки UI)
1.  Откройте `http://localhost:5173` в браузере.
2.  UI будет отображаться, но методы Telegram (например, закрытие окна) будут работать в режиме "заглушки". В консоли браузера вы увидите сообщения.

#### Вариант Б: Тестирование внутри Telegram (Tunneling)
Чтобы открыть приложение прямо внутри Telegram на телефоне или десктопе, ваш локальный сервер должен быть доступен из интернета по HTTPS.

1.  Используйте **ngrok** или **localtunnel** для проброса порта 5173:
    ```bash
    ngrok http 5173
    ```
2.  Скопируйте полученную https ссылку (например, `https://random-id.ngrok-free.app`).
3.  Откройте [@BotFather](https://t.me/BotFather) -> `/mybots` -> Выберите бота -> **Bot Settings** -> **Menu Button** -> **Configure Menu Button**.
4.  Отправьте BotFather ссылку из ngrok.
5.  Теперь при нажатии кнопки "Menu" или команды `/start` в вашем боте откроется локальная версия приложения.

> ⚠️ **Важно:** Бэкенд (API) также должен быть доступен для фронтенда. Если вы используете ngrok, убедитесь, что фронтенд обращается к API по правильному адресу. В текущей конфигурации `docker-compose`, фронтенд и бэкенд находятся в одной сети, но браузер клиента должен иметь доступ к API. Для локальных тестов можно пробросить и порт 8000 через ngrok и указать его в `.env` фронтенда (`VITE_API_URL`).

---

## 📊 Где смотреть статистику

На данном этапе (MVP) статистика доступна через:

1.  **Логи контейнеров:**
    Можно видеть кто заходит и генерирует истории.
    ```bash
    docker-compose logs -f backend
    ```

2.  **База данных (прямой доступ):**
    Подключитесь к БД (порт 5433 локально) любым клиентом (DBeaver, pgAdmin).
    - Таблица `users`: количество зарегистрированных пользователей.
    - Таблица `stories`: количество сгенерированных историй.

    Пример SQL запроса для статистики:
    ```sql
    SELECT count(*) FROM stories; -- Всего историй
    SELECT count(*) FROM users;   -- Всего пользователей
    ```

**В планах:** Разработка отдельной админ-панели с графиками (Grafana или custom admin).

---

## 📂 Структура проекта

```
contexta_tg/
├── backend/                # Серверная часть (FastAPI)
│   ├── app/
│   │   ├── api/            # API эндпоинты
│   │   ├── bot/            # Логика Telegram бота
│   │   ├── core/           # Конфигурация и безопасность
│   │   ├── models/         # SQLAlchemy модели БД
│   │   ├── services/       # Бизнес-логика (User, AI)
│   │   └── main.py         # Точка входа
│   ├── migrations/         # Файлы миграций Alembic
│   └── Dockerfile
├── frontend/               # Клиентская часть (React)
│   ├── src/
│   │   ├── components/     # UI компоненты
│   │   ├── pages/          # Страницы
│   │   └── App.tsx         # Роутинг
│   └── Dockerfile
├── docker-compose.yml      # Описание инфраструктуры
└── README.md               # Этот файл
```

## 🤝 Вклад в проект

1.  Форкните репозиторий.
2.  Создайте ветку для фичи (`git checkout -b feature/amazing-feature`).
3.  Закоммитьте изменения (`git commit -m 'Add amazing feature'`).
4.  Запушьте ветку (`git push origin feature/amazing-feature`).
5.  Откройте Pull Request.
