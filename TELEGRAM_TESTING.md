# 🧪 Тестирование в Telegram с локальным сервером

Чтобы протестировать Mini App прямо внутри Telegram, используя ваш локальный код, вам понадобится "туннель" (например, **ngrok**), чтобы сделать ваш локальный сервер доступным из интернета по HTTPS.

## 1. Установка ngrok

Если у вас нет ngrok, установите его:
[Скачать ngrok](https://ngrok.com/download)

Или через snap (Linux):
```bash
sudo snap install ngrok
```

## 2. Запуск туннелей

Нам нужно открыть доступ к двум портам:
*   **Frontend**: 5173
*   **Backend**: 8000

Лучше всего запустить два отдельных терминала или использовать конфигурационный файл ngrok.

**Терминал 1 (Frontend):**
```bash
ngrok http 5173
```
*Скопируйте полученный HTTPS адрес (например, `https://front-xyz.ngrok-free.app`)*

**Терминал 2 (Backend):**
```bash
ngrok http 8000
```
*Скопируйте полученный HTTPS адрес (например, `https://back-xyz.ngrok-free.app`)*

## 3. Настройка переменных окружения

Теперь нужно "подружить" фронтенд и бекенд через эти публичные адреса.

1.  Откройте файл `.env`.
2.  Найдите или добавьте переменную `VITE_API_URL` (она используется при сборке фронтенда, но для локальной разработки можно передать её при запуске).
    *   Так как мы используем Docker, проще всего поменять её в `docker-compose.yml` или создать `.env.local` для фронта, но самый быстрый способ — остановить контейнеры и отредактировать `docker-compose.yml`:

    В сервисе `frontend`:
    ```yaml
    environment:
      - VITE_API_URL=https://back-xyz.ngrok-free.app/api/v1 # <-- ВАШ BACKEND URL ИЗ NGROK
    ```
    *Важно: Не забудьте `/api/v1` в конце, если ваш код этого ожидает (проверьте `Home.tsx`).*

3.  Перезапустите Docker:
    ```bash
    docker-compose down
    docker-compose up -d --build
    ```

## 4. Настройка Telegram бота (BotFather)

1.  Откройте [@BotFather](https://t.me/BotFather) в Telegram.
2.  Выберите своего бота: `/mybots`.
3.  Перейдите в **Bot Settings** -> **Menu Button** (или **WebApp** настройки).
4.  Нажмите **Configure Menu Button** (или **Edit**).
5.  Вставьте **Frontend URL** из ngrok (например, `https://front-xyz.ngrok-free.app`).
6.  Сохраните.

## 5. Проверка (Важно!)

При использовании бесплатных тарифов ngrok, при первом открытии ссылки в браузере (или WebApp) появляется страница с предупреждением "You are about to visit...".

*   Telegram WebApp может **не открыться**, если там висит это предупреждение.
*   **Решение:** Откройте в обычном браузере на компьютере и телефоне ссылки `https://front-xyz.ngrok-free.app` и `https://back-xyz.ngrok-free.app/docs` и нажмите кнопку **"Visit Site"**, чтобы создать cookie, разрешающую доступ. (Или используйте платный ngrok / localtunnel / serveo).

Теперь можно открывать бота в Telegram и жать кнопку меню! 🚀

---

### Альтернатива (Localtunnel)
Если не хотите регистрироваться в ngrok:
```bash
npx localtunnel --port 5173
npx localtunnel --port 8000
```
Но будьте внимательны с их "Bypass Password" страницей.
